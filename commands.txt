-- store init --
/* used to initialize store in the repository */

-- store hash-content <file> --
/* used to hash the contents of file in file argument and store in .store/objects */

-- store cat-file <hash> --
/* used to return the contents of provided hash */

-- store add <file> --
/* adds a file to the index which can be commited */
/* use store add .  to stage all files */

-- store commit <message> --
/* commits the changes as per index.json and updates refs/heads/main with new hash */